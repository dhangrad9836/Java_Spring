# Use wildcard "*" to expose all endpoints
# Can also expose individual endpoints with a comma-delimited list
management.endpoints.web.exposure.include=*
management.info.env.enabled=true

#Exclude individual endpoints with a comma-delimited list
#   management.endpoints.web.exposure.exclude=health, info
info.app.name=My SUper Cool App
info.app.description=A crazy fun app, yahoo
info.app.version=1.0.0

#
# Define my custom properties
#

coach.name=Mickey Mouse
team.name=The Mouse Club


#
# Change Spring Boot embedded server port to 7070
#

server.port=7070

#
# Set the context path of the application
#
# All requests should be prefixed with /mycoolapp
# So basically your new url path would be localhost:8080/mycoolapp/info....you have to specify mycoolapp before any endpoints
#

server.servlet.context-path=/mycoolapp
















